{
  "name": "impeccable-unisex-salon-saas",
  "version": "1.0.0",
  "private": true,
  "description": "Premium MERN digital service cart SaaS for Impeccable Unisex Salon.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "keywords": ["mern", "salon", "luxury", "service-cart", "saas"],
  "author": "Impeccable Unisex Salon",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}